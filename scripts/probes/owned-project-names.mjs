#!/usr/bin/env node
// Name/alias acceptance is now part of the account-aware owned-project fixture.
// Requires a fresh disposable installed STATE and ENGINE_REPO; no paid login.
await import('./owned-accounts.mjs');
